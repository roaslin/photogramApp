describe('PostsRepository should', () => {
  test('return empty posts when user is not following anyone', () => {
    // const result =
    const caca = 'daad';

    expect('true').toEquals(caca);
  });
});
